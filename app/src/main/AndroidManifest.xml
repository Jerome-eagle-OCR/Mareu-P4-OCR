<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lamzone.mareu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.Maréu">
        <activity
            android:name=".ui.meetings_list.NewMeetingActivity"
            android:parentActivityName=".ui.meetings_list.MeetingsListActivity"
            android:theme="@style/Theme.Maréu.NoActionBar" />
        <activity android:name=".ui.meetings_list.MeetingsListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>